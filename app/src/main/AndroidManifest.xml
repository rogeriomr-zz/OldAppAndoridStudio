<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="old.studio.com.old_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Cadastro"
            android:label="@string/cadastro" />
        <activity android:name=".cadastro_Especifico" />
        <activity
            android:name=".CadastroMedicamentos"
            android:label="@string/cadastromedicamentos" />
        <activity
            android:name=".Principal"
            android:label="@string/menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Medicamentos"
            android:label="@string/medicamentos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Configuracao"
            android:label="@string/configuracoes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".editar_Dados"
            android:label="@string/editardados" />
        <activity android:name=".consultar_Medicamentos" />
        <activity android:name=".trocar_Senha"
                    android:label="@string/trocarsenha"></activity>
    </application>

</manifest>